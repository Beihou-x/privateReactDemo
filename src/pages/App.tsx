import React from 'react';
import RenderRoute from '@components/RenderRoute';
import '../index.less';

const App = props => {
    return (
        <RenderRoute {...props}></RenderRoute>
    )
};

export default App;